package frc.robot.subsystems.superstructure.effector;

class Effector {}

// Open loop until that rear beam break is broken, then we know the game piece is sticking out x inches. then reset position of encoder and use PID to control corals position within the end effector
